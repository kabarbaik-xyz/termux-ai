# Samples — One Project Through Every Phase
Fictional client **KirimKilat Logistik** (parcel courier) flows through the whole pipeline.
Walk your team through IN ORDER (00→10) and point at the ID chain:
`SRC-1 → BO-1 → US-101 → SC-01 → TrackingLookup.vue → tracking.spec.ts` — one requirement, traceable from the client's email to the test file.
Each sample is a realistic EXCERPT (real docs run longer). Bilingual on purpose: client speaks ID, artifacts cite verbatim.
